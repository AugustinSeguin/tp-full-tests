import { Hour, Cadran } from "./cadran";

/*
  Return the time by moon, sun & earth values
  @param moon - The moon value
  @param sun - The sun value
  @param earth - The earth value
  @returns The time INTEGER
*/
export function getTime(moon: number, sun: number, earth: number): Hour {
  return Hour.Mortin;
}
